version: '3.8'

services:
  whatsapp-bot:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
